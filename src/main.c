int main (/*int argc, char ** argv*/) {
	/*
	FILE * pa;
	pa = fopen("geracoes.txt", a+);
	Individuo * populacao, melhor;
	int n_geracao = 0, n_individuos = POP_INICIAL;

	populacao = inicia_populacao();
	exporta_geracao(populacao, n_individuos, pa, n_geracao);

	for (int i = 0; i < GERACAO_SECCAO; i++) {
		n_geracao++;
		populacao = reproduz_individuos();
		exporta_geracao(populacao, n_individuos, pa, n_geracao);
	}

	libera_individuos(populacao);
	fclose(pa);
	*/
	return 0;
}
